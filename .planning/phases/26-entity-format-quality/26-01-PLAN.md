---
phase: 26-entity-format-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/memory/entity-normalizer.ts
  - apps/api/src/memory/__tests__/entity-normalizer.test.ts
  - apps/api/src/memory/prompts.ts
autonomous: true
requirements: [FMT-01, ENT-01, ENT-02, ENT-03, ENT-04, ENT-05]

must_haves:
  truths:
    - "Entities with hallucinated types (greeting, schedule, time, amount) are mapped to canonical types"
    - "Garbage values (empty strings, single chars, pronouns, URLs, generic terms) are stripped"
    - "Duplicate entities within a single memory are collapsed to one entry"
    - "Entity count is capped at 30 per memory"
    - "Both embed-shape {type, id, role} and enrich-shape {type, value} entities produce unified {type, value} output"
    - "Extraction prompt includes negative examples and connector-aware hints"
  artifacts:
    - path: "apps/api/src/memory/entity-normalizer.ts"
      provides: "normalizeEntities() pure function, CANONICAL_ENTITY_TYPES, TYPE_MAP, NormalizedEntity type"
      exports: ["normalizeEntities", "CANONICAL_ENTITY_TYPES", "NormalizedEntity"]
    - path: "apps/api/src/memory/__tests__/entity-normalizer.test.ts"
      provides: "Unit tests covering type mapping, garbage filtering, dedup, cap, format unification"
      min_lines: 80
    - path: "apps/api/src/memory/prompts.ts"
      provides: "Updated ENTITY_FORMAT_SCHEMA with new canonical types, improved entityExtractionPrompt"
      contains: "concept"
  key_links:
    - from: "apps/api/src/memory/entity-normalizer.ts"
      to: "apps/api/src/memory/__tests__/entity-normalizer.test.ts"
      via: "import"
      pattern: "import.*normalizeEntities.*entity-normalizer"
---

<objective>
Create the entity normalizer pure function and update the extraction prompt with the new canonical taxonomy.

Purpose: Establish the normalization layer that all entity extraction flows will use. The normalizer validates types against the canonical 10-type taxonomy, strips garbage values, deduplicates by type+value, and caps entity count. The prompt update ensures the LLM targets the correct types.

Output: `entity-normalizer.ts` (tested pure function), updated `prompts.ts` (new schema + improved prompt)
</objective>

<execution_context>
@/Users/amr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-entity-format-quality/26-RESEARCH.md

<interfaces>
<!-- Current prompts.ts exports that will be modified -->
From apps/api/src/memory/prompts.ts:
```typescript
export const ENTITY_FORMAT_SCHEMA = {
  type: 'object',
  properties: {
    entities: {
      type: 'array',
      items: {
        type: 'object',
        properties: {
          type: {
            type: 'string',
            enum: ['person', 'organization', 'location', 'event', 'product', 'topic', 'pet', 'group', 'device', 'other'],
          },
          value: { type: 'string' },
        },
        required: ['type', 'value'],
      },
    },
  },
  required: ['entities'],
};
export function entityExtractionPrompt(text: string): string;
```

<!-- EmbedResult shape from connector-sdk (DO NOT MODIFY -- used for contact resolution) -->
From packages/connector-sdk/src/types.ts:
```typescript
export interface EmbedResult {
  text: string;
  entities: Array<{ type: string; id: string; role: string }>;
  metadata?: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create entity normalizer with tests</name>
  <files>apps/api/src/memory/entity-normalizer.ts, apps/api/src/memory/__tests__/entity-normalizer.test.ts</files>
  <behavior>
    - normalizeEntities([{type:'greeting', value:'Hello'}]) returns [{type:'other', value:'Hello'}] (type mapping)
    - normalizeEntities([{type:'topic', value:'AI'}]) returns [{type:'concept', value:'AI'}] (topic->concept)
    - normalizeEntities([{type:'pet', value:'Rex'}]) returns [{type:'other', value:'Rex'}] (pet->other)
    - normalizeEntities([{type:'group', value:'Engineering'}]) returns [{type:'organization', value:'Engineering'}] (group->organization)
    - normalizeEntities([{type:'device', value:'iPhone'}]) returns [{type:'product', value:'iPhone'}] (device->product)
    - normalizeEntities([{type:'person', value:''}]) returns [] (empty value stripped)
    - normalizeEntities([{type:'person', value:'I'}]) returns [] (single char stripped)
    - normalizeEntities([{type:'person', value:'you'}]) returns [] (pronoun stripped)
    - normalizeEntities([{type:'other', value:'hello'}]) returns [] (generic term stripped)
    - normalizeEntities([{type:'other', value:'https://example.com'}]) returns [] (URL stripped)
    - normalizeEntities([{type:'person', value:'John'}, {type:'person', value:'John'}]) returns [{type:'person', value:'John'}] (dedup)
    - normalizeEntities([{type:'person', value:'John'}, {type:'person', value:'john'}]) returns [{type:'person', value:'John'}] (case-insensitive dedup, keeps first)
    - normalizeEntities(Array(50).fill({type:'person', value:'Name'+i}), 30) returns array of length 30 (cap)
    - normalizeEntities([{type:'person', id:'name:John|email:john@x.com', role:'sender'}]) handles embed-shape input by extracting value from id field
    - All 10 canonical types pass through unchanged: person, organization, location, date, event, product, concept, quantity, language, other
  </behavior>
  <action>
    1. Create `apps/api/src/memory/__tests__/entity-normalizer.test.ts` first with failing tests covering all behaviors above.
    2. Create `apps/api/src/memory/entity-normalizer.ts` implementing:
       - Export `CANONICAL_ENTITY_TYPES` as const array: `['person', 'organization', 'location', 'date', 'event', 'product', 'concept', 'quantity', 'language', 'other']` (lowercase per codebase convention -- research recommends lowercase to avoid breaking contacts system)
       - Export `NormalizedEntity` interface: `{ type: CanonicalEntityType; value: string }`
       - Export `TYPE_MAP` mapping old/hallucinated types to canonical: topic->concept, pet->other, group->organization, device->product, greeting->other, schedule->event, time->date, amount->quantity, metric->quantity, currency->quantity, email->other, url->other
       - Export `normalizeEntities(raw, maxEntities=30)` pure function that:
         a. Extracts value from various shapes: `entity.value ?? entity.name ?? entity.id` (for embed entities, parse compound id format `name:X|email:Y` to extract the name part)
         b. Strips garbage: empty, length<=1, pronouns (I/you/he/she/it/we/they/me/him/her/us/them + possessives), generic terms (hello/hi/hey/thanks/bye/ok/yes/no/maybe/sure/please/the/a/an/this/that), bare URLs (`^https?://`)
         c. Maps type to canonical via TYPE_MAP, defaults unknown types to 'other'
         d. Deduplicates by `${type}::${value.toLowerCase()}`
         e. Caps at maxEntities
    3. Run tests -- all must pass.
  </action>
  <verify>
    <automated>cd /Users/amr/Projects/botmem && pnpm --filter @botmem/api test -- --run apps/api/src/memory/__tests__/entity-normalizer.test.ts</automated>
  </verify>
  <done>normalizeEntities() handles type mapping (5+ old types), garbage filtering (pronouns, URLs, generic terms, empty/short), case-insensitive dedup, entity cap, and both embed-shape and enrich-shape input formats. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Update entity extraction prompt and schema</name>
  <files>apps/api/src/memory/prompts.ts</files>
  <action>
    1. Update `ENTITY_FORMAT_SCHEMA.properties.entities.items.properties.type.enum` to the new canonical types: `['person', 'organization', 'location', 'date', 'event', 'product', 'concept', 'quantity', 'language', 'other']`
    2. Rewrite `entityExtractionPrompt(text)` with:
       - Clear instruction to extract named entities only (not actions, greetings, or generic nouns)
       - Updated allowed types list matching the new schema
       - Negative examples: "Do NOT extract: greetings ('hello', 'thanks'), pronouns ('I', 'you'), generic terms, bare URLs, single characters"
       - Positive examples per type:
         - person: "John Smith", "Dr. Lee"
         - organization: "Google", "UN", "Engineering Team"
         - location: "New York", "Building 42", "Japan"
         - date: "March 2024", "next Tuesday", "Q3"
         - event: "board meeting", "product launch", "wedding"
         - product: "iPhone 15", "Slack", "MacBook"
         - concept: "machine learning", "agile methodology", "budget"
         - quantity: "$500", "3 million users", "50%"
         - language: "English", "Python", "Arabic"
         - other: only when no other type fits
       - Keep the instruction about not extracting time references as separate metadata (they go under 'date' type only if they are named temporal references, not relative ones like "yesterday")
       - Add: "Extract at most 30 entities. Focus on the most important ones."
    3. Verify existing tests still pass (the prompt change should not break anything since the normalizer post-processes anyway).
  </action>
  <verify>
    <automated>cd /Users/amr/Projects/botmem && pnpm --filter @botmem/api test -- --run</automated>
  </verify>
  <done>ENTITY_FORMAT_SCHEMA uses new canonical 10-type enum. entityExtractionPrompt includes positive and negative examples, type descriptions, and entity cap instruction.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @botmem/api test -- --run` passes (all existing + new tests)
- `entity-normalizer.ts` exports normalizeEntities, CANONICAL_ENTITY_TYPES, NormalizedEntity
- `prompts.ts` ENTITY_FORMAT_SCHEMA enum matches CANONICAL_ENTITY_TYPES
- No references to old types (topic, pet, group, device) remain in prompts.ts enum
</verification>

<success_criteria>
1. Entity normalizer is a tested pure function that handles type mapping, garbage filtering, dedup, and capping
2. ENTITY_FORMAT_SCHEMA uses the new 10-type canonical taxonomy
3. Entity extraction prompt includes positive/negative examples and stricter instructions
4. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-entity-format-quality/26-01-SUMMARY.md`
</output>
