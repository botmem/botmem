---
phase: 28-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VER-01, VER-02, VER-03, VER-04]

must_haves:
  truths:
    - 'Photo search returns only photo-type memories, zero Slack file attachments'
    - "NLQ query 'show me photos from last week' uses source_type: photo directly without alias resolution"
    - 'Memory graph entities use only canonical types (person, organization, location, date, event, product, concept, quantity, language, other)'
    - 'No garbage entity nodes in graph (no pronouns, single chars, URLs, generic terms)'
    - 'No duplicate entities within a single memory in graph view'
  artifacts: []
  key_links:
    - from: 'NLQ parser SOURCE_TYPE_MAP'
      to: 'memory.service.ts effectiveFilters.sourceType'
      via: 'sourceTypeHint passthrough'
      pattern: "effectiveFilters\\.sourceType.*nlq\\.sourceTypeHint"
    - from: 'entity-normalizer.ts'
      to: 'enrich.service.ts'
      via: 'normalizeEntities() call'
      pattern: 'normalizeEntities'
---

<objective>
Verify end-to-end data quality after Phases 25-27 fixes (source type reclassification, entity normalization, data backfill).

Purpose: Confirm that search, graph visualization, and NLQ queries produce correct, clean results -- closing the v2.1 Data Quality milestone.
Output: Documented pass/fail evidence for VER-01 through VER-04.
</objective>

<execution_context>
@/Users/amr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-verification/28-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated API verification of VER-01, VER-02, VER-03, VER-04</name>
  <files></files>
  <action>
Run live API queries against the running dev system to verify all four requirements. No code changes -- this is pure verification.

**Pre-flight:**

1. Check `GET http://localhost:12412/api/health` to confirm services are up. If not, tell the user and stop.
2. Log in via `POST http://localhost:12412/api/user-auth/login` with `{"email":"amroessams@gmail.com","password":"password123"}` to get a JWT access token.
3. Check backfill completeness: query SQLite for `SELECT COUNT(*) FROM memories WHERE enrichedAt IS NULL` -- if significant number remain un-enriched, note it as a caveat.

**VER-01: Fresh pipeline correctness (source types + clean entities)**

- Query `POST http://localhost:12412/api/memories/search` with body `{"query":"recent","limit":20}` and auth header.
- Inspect results: every memory should have a valid `sourceType` (email, message, photo, etc.), not `file` for photo content.
- Inspect `entities` field (JSON string): parse and check that all entity types are from the canonical set (person, organization, location, date, event, product, concept, quantity, language, other).
- Check that no entity values are empty strings, single characters, pronouns (I, you, he, she, they), or bare URLs.
- NOTE: A full re-sync is NOT required if backfilled data already demonstrates correct pipeline behavior. Check recent memories that were enriched (enrichedAt is not NULL).

**VER-02: Photo search isolation**

- Query `POST http://localhost:12412/api/memories/search` with body `{"query":"photos"}` and auth header.
- Assert: ALL returned results have `sourceType: "photo"`. NONE have `sourceType: "file"` or `connectorType: "slack"`.
- If no photo memories exist in the database, note this as "SKIP -- no photo data available" (not a failure).

**VER-03: Entity graph cleanliness**

- Query `GET http://localhost:12412/api/memories/graph` with auth header.
- Inspect entity-related data in nodes: check for canonical types only, no duplicates within a single memory's entities, no garbage values.
- Sample at least 5 memory nodes and inspect their entities.

**VER-04: NLQ photo source type**

- Query `POST http://localhost:12412/api/memories/search` with body `{"query":"show me photos from last week"}` and auth header.
- Inspect the `parsed` field in the response: `parsed.sourceTypeHint` should be `"photo"`.
- Confirm no alias resolution occurred (no `SOURCE_TYPE_ALIASES` reference in response).

**Document results** as a structured pass/fail table with evidence snippets for each VER requirement. If any check fails, document the failure with specifics (what was expected vs. what was found) but do NOT attempt to fix code -- just document.
</action>
<verify>
<automated>curl -s http://localhost:12412/api/health | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const j=JSON.parse(d);process.exit(j.sqlite?.connected && j.redis?.connected && j.qdrant?.connected ? 0 : 1)"</automated>
</verify>
<done>All four VER checks have documented pass/fail status with evidence. Any failures are documented with specifics, not silently ignored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Automated verification of all data quality requirements (VER-01 through VER-04) with documented evidence</what-built>
  <how-to-verify>
    1. Review the verification results table from Task 1
    2. Open http://localhost:12412 in browser, navigate to Memory Explorer
    3. Search for "photos" -- confirm results are only photo-type memories
    4. Open the Graph view -- visually confirm:
       - Entity nodes show clean labels (real names, places, organizations)
       - No garbage nodes (single letters, "hello", "thanks", URLs)
       - Entity types are recognizable canonical categories
    5. Search "show me photos from last week" -- confirm NLQ correctly filters to photos
    6. If any VER check failed in Task 1, decide whether to:
       a. Accept as-is (data limitation, not a code bug)
       b. File as a bug for future fix
       c. Request immediate fix (will need a gap closure plan)
  </how-to-verify>
  <resume-signal>Type "approved" to mark v2.1 milestone complete, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All four VER requirements verified with documented evidence:
- VER-01: Source types correct, entities clean in recent/backfilled memories
- VER-02: Photo search returns only photos
- VER-03: Graph entities are canonical, deduplicated, no garbage
- VER-04: NLQ photo queries use photo source type directly
</verification>

<success_criteria>

- All 4 VER checks pass OR failures are documented with root cause
- Human confirms graph visual quality in checkpoint
- v2.1 Data Quality milestone can be marked complete (or gap closure plans identified)
  </success_criteria>

<output>
After completion, create `.planning/phases/28-verification/28-01-SUMMARY.md`
</output>
