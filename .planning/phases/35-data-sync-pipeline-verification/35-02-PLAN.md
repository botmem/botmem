---
phase: 35-data-sync-pipeline-verification
plan: 02
type: execute
wave: 2
depends_on: ['35-01']
files_modified:
  - packages/connector-sdk/src/base.ts
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03

must_haves:
  truths:
    - 'Gmail sync produces raw email events and memories with source_type=email, resolved contacts, entities, factuality, and Qdrant vectors'
    - 'Slack sync produces raw message events and memories with source_type=message, resolved contacts, entities, factuality, and Qdrant vectors'
    - 'WhatsApp sync produces raw message events and memories with source_type=message, participant contacts, entities, factuality, and Qdrant vectors'
    - 'Verification script reports PASS for all checks on gmail, slack, and whatsapp connectors'
  artifacts:
    - path: 'scripts/verify-pipeline.ts'
      provides: 'Pipeline verification passes for gmail, slack, whatsapp'
  key_links:
    - from: 'connector.sync()'
      to: 'rawEvents table'
      via: 'SyncProcessor'
      pattern: 'raw_events.*connector_type'
    - from: 'rawEvents'
      to: 'memories'
      via: 'EmbedProcessor + EnrichProcessor'
      pattern: 'pipeline_complete.*true'
---

<objective>
Wipe dev database for clean validation, set DEBUG_SYNC_LIMIT=50, then sync Gmail, Slack, and WhatsApp sequentially. After each connector sync completes (all jobs finish), run the verification script to confirm full pipeline correctness. Fix any issues found before proceeding to the next connector.

Purpose: Validate the 3 most complex connectors (OAuth, rich data) produce correct end-to-end pipeline output.
Output: 3 connectors synced with all pipeline verification checks passing.
</objective>

<execution_context>
@/Users/amr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-data-sync-pipeline-verification/35-CONTEXT.md
@.planning/phases/35-data-sync-pipeline-verification/35-01-SUMMARY.md
@packages/connector-sdk/src/base.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh database + set DEBUG_SYNC_LIMIT=50</name>
  <files>packages/connector-sdk/src/base.ts</files>
  <action>
1. Set `DEBUG_SYNC_LIMIT = 50` in `packages/connector-sdk/src/base.ts` (line 18, change `= 0` to `= 50`).

2. Wipe existing dev data for clean validation. Run SQL against the dev PostgreSQL database:
   - `TRUNCATE memories, memory_contacts, memory_links, contacts, contact_identifiers, raw_events, jobs, logs CASCADE;`
   - Also clear Qdrant: `curl -X DELETE http://localhost:6333/collections/memories` (the collection will be auto-recreated on next upsert)

3. Build the connector-sdk so the limit takes effect: `cd packages/connector-sdk && pnpm build`

4. Verify the API is running and picks up the change (check `GET /api/version`).

NOTE: Do NOT commit the DEBUG_SYNC_LIMIT change — it will be reverted at the end of Phase 35.
</action>
<verify>
<automated>cd /Users/amr/Projects/botmem && grep "DEBUG_SYNC_LIMIT = 50" packages/connector-sdk/src/base.ts && echo "PASS"</automated>
</verify>
<done>Dev database is empty, Qdrant collection cleared, DEBUG_SYNC_LIMIT set to 50, connector-sdk rebuilt</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sync Gmail + Slack + WhatsApp and verify pipeline</name>
  <what-built>
For each connector (Gmail, Slack, WhatsApp) in order:

**Gmail (SYNC-01):**

1. Trigger Gmail sync via UI or API: `POST /api/jobs` with `{ "connectorType": "gmail", "accountId": "<gmail-account-id>" }`
2. Wait for all jobs (sync → embed → enrich) to complete — monitor via WebSocket or poll `/api/jobs`
3. Run verification: `npx tsx scripts/verify-pipeline.ts --connector gmail`
4. If any check FAILs: investigate and fix the issue before proceeding

**Slack (SYNC-02):**

1. Trigger Slack sync via UI or API with the configured Slack account
2. Wait for all pipeline jobs to complete
3. Run verification: `npx tsx scripts/verify-pipeline.ts --connector slack`
4. Fix any failures before proceeding

**WhatsApp (SYNC-03):**

1. Trigger WhatsApp sync (may need QR re-auth if session expired)
2. Wait for all pipeline jobs to complete
3. Run verification: `npx tsx scripts/verify-pipeline.ts --connector whatsapp`
4. Fix any failures

After all 3 connectors pass, run the full verification: `npx tsx scripts/verify-pipeline.ts`
</what-built>
<how-to-verify>

1. Check the verification script output — all checks for gmail, slack, whatsapp should show PASS
2. In the web UI, search for a term you know exists in your Gmail — it should return email results
3. Search for a Slack message you remember — it should return message results
4. Search for a WhatsApp message — it should return message results
5. Verify the memory graph shows contacts connected to memories
   </how-to-verify>
   <resume-signal>Type "approved" if all 3 connectors pass verification, or describe which connector/check failed</resume-signal>
   </task>

</tasks>

<verification>
- `npx tsx scripts/verify-pipeline.ts --connector gmail` — all PASS
- `npx tsx scripts/verify-pipeline.ts --connector slack` — all PASS
- `npx tsx scripts/verify-pipeline.ts --connector whatsapp` — all PASS
- Search returns results from each connector type
</verification>

<success_criteria>

- Gmail sync produces email memories with contacts, entities, factuality, Qdrant vectors
- Slack sync produces message memories with contacts, entities, factuality, Qdrant vectors
- WhatsApp sync produces message memories with participant contacts, entities, factuality, Qdrant vectors
- All verification checks pass for all 3 connectors
  </success_criteria>

<output>
After completion, create `.planning/phases/35-data-sync-pipeline-verification/35-02-SUMMARY.md`
</output>
