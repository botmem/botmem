---
phase: 35-data-sync-pipeline-verification
plan: 03
type: execute
wave: 3
depends_on: ['35-02']
files_modified:
  - packages/connector-sdk/src/base.ts
autonomous: false
requirements:
  - SYNC-04
  - SYNC-05
  - SYNC-06

must_haves:
  truths:
    - 'iMessage sync produces raw message events and memories with source_type=message, contacts, entities, factuality, and Qdrant vectors'
    - 'Photos-Immich sync produces raw photo events and memories with source_type=photo, location entities, and Qdrant vectors'
    - 'Locations (OwnTracks) sync produces raw GPS events and memories with source_type=location, coordinates, and Qdrant vectors'
    - 'Verification script reports PASS for all checks across ALL 6 connectors'
    - 'DEBUG_SYNC_LIMIT is reset to 0 after validation'
  artifacts:
    - path: 'packages/connector-sdk/src/base.ts'
      provides: 'DEBUG_SYNC_LIMIT reset to 0'
      contains: 'DEBUG_SYNC_LIMIT = 0'
  key_links:
    - from: 'connector.sync()'
      to: 'memories'
      via: 'full pipeline'
      pattern: 'pipeline_complete.*true'
---

<objective>
Sync iMessage, Photos-Immich, and Locations (OwnTracks) connectors sequentially, verify full pipeline correctness for each, then run final all-connector verification and reset DEBUG_SYNC_LIMIT to 0.

Purpose: Complete validation of all 6 connectors and leave codebase in clean state.
Output: All 6 connectors verified, DEBUG_SYNC_LIMIT reset, full pipeline validation passing.
</objective>

<execution_context>
@/Users/amr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-data-sync-pipeline-verification/35-CONTEXT.md
@.planning/phases/35-data-sync-pipeline-verification/35-01-SUMMARY.md
@.planning/phases/35-data-sync-pipeline-verification/35-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Sync iMessage + Photos-Immich + Locations and verify pipeline</name>
  <what-built>
For each connector in order:

**iMessage (SYNC-04):**

1. iMessage is a local-tool connector — trigger sync via UI or API
2. Wait for all pipeline jobs to complete
3. Run: `npx tsx scripts/verify-pipeline.ts --connector imessage`
4. Fix any failures before proceeding

**Photos-Immich (SYNC-05):**

1. Trigger Photos-Immich sync (requires Immich server URL + API key configured)
2. Wait for all pipeline jobs to complete
3. Run: `npx tsx scripts/verify-pipeline.ts --connector photos-immich`
4. Fix any failures before proceeding

**Locations / OwnTracks (SYNC-06):**

1. Trigger OwnTracks sync using configured credentials (base URL: https://owntracks.home.covidvpn.me, user: amr, device: iphone)
2. Wait for all pipeline jobs to complete
3. Run: `npx tsx scripts/verify-pipeline.ts --connector owntracks`
4. Fix any failures

**Final full verification:**
Run: `npx tsx scripts/verify-pipeline.ts` (no --connector flag = check ALL connectors)
All 6 connectors should show all PASS.
</what-built>
<how-to-verify>

1. Full verification script output shows PASS for all checks across all 6 connectors
2. Search for a photo caption — returns photo results
3. Search for a location name — returns location results
4. Search for an iMessage conversation — returns message results
5. Memory graph shows contacts from all connector types
   </how-to-verify>
   <resume-signal>Type "approved" if all 6 connectors pass full verification, or describe failures</resume-signal>
   </task>

<task type="auto">
  <name>Task 2: Reset DEBUG_SYNC_LIMIT and commit</name>
  <files>packages/connector-sdk/src/base.ts</files>
  <action>
1. Reset `DEBUG_SYNC_LIMIT = 0` in `packages/connector-sdk/src/base.ts` (line 18, change `= 50` back to `= 0`).

2. Rebuild connector-sdk: `cd packages/connector-sdk && pnpm build`

3. Run `pnpm build` to verify everything builds clean.

4. Commit all Phase 35 code changes:
   - `apps/api/src/db/schema.ts` (pipelineComplete column)
   - `apps/api/src/memory/enrich.processor.ts` (set pipelineComplete=true)
   - `apps/api/src/memory/memory.service.ts` (filter pipelineComplete in queries)
   - `scripts/verify-pipeline.ts` (verification script)
   - `packages/connector-sdk/src/base.ts` (DEBUG_SYNC_LIMIT back to 0)
   - Any pipeline bug fixes made during sync validation
     </action>
     <verify>
     <automated>cd /Users/amr/Projects/botmem && grep "DEBUG_SYNC_LIMIT = 0" packages/connector-sdk/src/base.ts && pnpm build 2>&1 | tail -3</automated>
     </verify>
     <done>DEBUG_SYNC_LIMIT reset to 0, all code changes committed, build passes</done>
     </task>

</tasks>

<verification>
- `npx tsx scripts/verify-pipeline.ts` — ALL connectors, ALL checks PASS
- `grep "DEBUG_SYNC_LIMIT = 0" packages/connector-sdk/src/base.ts` — confirmed reset
- `pnpm build` — passes cleanly
- Git working tree clean (all changes committed)
</verification>

<success_criteria>

- All 6 connectors synced and pipeline-verified
- Verification script passes for gmail, slack, whatsapp, imessage, photos-immich, owntracks
- DEBUG_SYNC_LIMIT reset to 0
- All code changes committed
  </success_criteria>

<output>
After completion, create `.planning/phases/35-data-sync-pipeline-verification/35-03-SUMMARY.md`
</output>
